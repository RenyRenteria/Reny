; Keep transport slightly above the application limit so Laravel can return the
; clear 1 GB validation error instead of PHP dropping the upload first.
upload_max_filesize = 2G

; Community posts allow up to 12 attachments in one request.
post_max_size = 13G
max_input_time = 3600
